# GOVERNED_BY: MASTER_OVERRIDING_INSTRUCTION.md
name: Master Governance

on:
  pull_request:
    paths:
      - 'MASTER_OVERRIDING_INSTRUCTION.md'
      - 'scripts/**'
      - '.github/workflows/**'
  push:
    branches: [main]
    paths:
      - 'MASTER_OVERRIDING_INSTRUCTION.md'
      - 'scripts/**'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  enforce-master-governance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Enforce final overriding master instruction on new processes
        run: python3 scripts/check_master_governance.py
