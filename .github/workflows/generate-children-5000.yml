name: Source-only children story policy

on:
  workflow_dispatch:

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The former 5,000-story synthetic generator is retired.'
          echo 'Children stories may be published only when their text is extracted from or explicitly grounded in site resources with source references.'
