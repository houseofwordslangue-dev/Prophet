name: Source-only animated children story policy

on:
  workflow_dispatch:

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The former 600-story synthetic generator is retired.'
          echo 'Animated children stories may be published only when their text and scenes are extracted from or explicitly grounded in site resources with source references.'
