name: Source-only very-short children story policy

on:
  workflow_dispatch:

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The former 500-story synthetic generator is retired.'
          echo 'Very-short children stories may be published only when their text is extracted from or explicitly grounded in site resources with source references.'
