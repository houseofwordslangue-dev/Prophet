name: Publish 100 genuine source articles

on:
  push:
    branches: [main]
    paths:
      - 'scripts/build_100_genuine_articles.py'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: prophet-editorial-source-publication
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build and validate exactly 100 source-extracted articles
        run: python scripts/build_100_genuine_articles.py
      - name: Commit generated publication batch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'prophet-editorial-bot'
          git config user.email 'prophet-editorial-bot@users.noreply.github.com'
          git add -- \
            data/editorial/drafts/2026-08-21 \
            data/editorial/publication_supplement.json \
            data/editorial/source_extract_100_audit.json \
            service-worker.js
          if git diff --cached --quiet; then
            echo 'No publication changes required.'
            exit 0
          fi
          git commit -m 'Publish 100 genuine source-extracted articles'
          for attempt in 1 2 3 4; do
            if git pull --rebase origin main && git push origin HEAD:main; then
              exit 0
            fi
            echo "Push attempt $attempt failed; retrying after refresh"
            git rebase --abort 2>/dev/null || true
            git fetch origin main
            git rebase origin/main
            sleep $((attempt * 3))
          done
          echo 'Unable to publish after four serialized retries.' >&2
          exit 1
