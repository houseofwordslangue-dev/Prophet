# Published

Control metadata for resources already available on the site. Public binaries remain canonical under `library/works/<workId>/editions/<editionId>/original.<format>` so reader URLs and running workflows are not broken.
