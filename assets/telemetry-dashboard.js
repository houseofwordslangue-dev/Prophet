(function(){'use strict';const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){try{const r=await fetch('/api/telemetry/summary',{cache:'no-store'}),j=await r.json();$('#events').textContent=j.events??0;$('#startup').textContent=j.avgStartupMs??0;$('#buffer').textContent=j.avgBufferMs??0;$('#search').textContent=j.avgSearchMs??0;$('#byEvent').innerHTML=Object.entries(j.byEvent||{}).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div><span>${esc(k)}</span><strong>${v}</strong></div>`).join('')||'<p>لا بيانات بعد.</p>';$('#lastRows').innerHTML=(j.last||[]).slice().reverse().map(x=>`<tr><td>${esc(x.event||'')}</td><td>${esc(x.path||'')}</td><td>${esc(x.startupMs??x.bufferMs??x.renderMs??x.searchMs??x.results??'—')}</td><td>${x.serverTs?new Date(x.serverTs).toLocaleString():'—'}</td></tr>`).join('')}catch(e){$('#byEvent').innerHTML='<p>يتطلب العرض تشغيل server.py.</p>'}}document.querySelector('#refresh').onclick=load;load();setInterval(load,30000)})();