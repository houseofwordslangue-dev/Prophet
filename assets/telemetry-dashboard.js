(function(){'use strict';const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){try{const r=await fetch('/api/telemetry/summary',{cache:'no-store'}),j=await r.json(),ev=j.byEvent||{},hits=Number(ev.cache_hit||0)+Number(ev.mirror_hit||0),miss=Number(ev.cache_miss||0),den=hits+miss;$('#events').textContent=j.events??0;$('#startup').textContent=j.avgStartupMs??0;$('#buffer').textContent=j.avgBufferMs??0;$('#search').textContent=j.avgSearchMs??0;$('#cacheRate').textContent=den?Math.round(hits/den*100)+'%':'—';$('#byEvent').innerHTML=Object.entries(ev).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div><span>${esc(k)}</span><strong>${v}</strong></div>`).join('')||'<p>لا بيانات بعد.</p>';$('#lastRows').innerHTML=(j.last||[]).slice().reverse().map(x=>`<tr><td>${esc(x.event||'')}</td><td>${esc(x.path||'')}</td><td>${esc(x.startupMs??x.bufferMs??x.renderMs??x.searchMs??x.results??'—')}</td><td>${x.serverTs?new Date(x.serverTs).toLocaleString():'—'}</td></tr>`).join('')}catch(e){$('#byEvent').innerHTML='<p>يتطلب العرض تشغيل server.py.</p>'}}document.querySelector('#refresh').onclick=load;load();setInterval(load,30000)})();